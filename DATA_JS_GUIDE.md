# 📘 demo.html을 활용한 data.js 생성 및 GitHub 업데이트 가이드

이 설명서는 **처음 페이지를 사용하는 사용자**를 위해 작성된 초보자 맞춤형 가이드입니다.  
`demo.html` 페이지의 데이터 변환기를 활용해 위치 데이터를 생성하고, 지도 좌표를 찍은 후 **`data.js`** 파일로 저장하여 **GitHub에 반영하는 전체 과정**을 단계별로 안내합니다.

---

## 📑 목차
1. [사전 준비 사항](#1-사전-준비-사항)
2. [1단계: demo.html 변환기 접속](#2-1단계-demohtml-변환기-접속)
3. [2단계: 엑셀 데이터 입력 및 변환](#3-2단계-엑셀-데이터-입력-및-변환)
4. [3단계: 지도 좌표 및 마커 설정](#4-3단계-지도-좌표-및-마커-설정)
5. [4단계: 결과 복사 및 data.js 파일 저장](#5-4단계-결과-복사-및-datajs-파일-저장)
6. [5단계: GitHub에 업데이트 반영하기](#6-5단계-github에-업데이트-반영하기)
7. [❓ 자주 묻는 질문 (FAQ)](#7--자주-묻는-질문-faq)

---

## 1. 사전 준비 사항

- **웹 브라우저**: Chrome, Edge, Safari 등 최신 브라우저
- **수정할 데이터 (엑셀 또는 기존 data.js)**:
  - 엑셀 데이터는 아래 **5개 열(Column)** 순서로 구성되어야 합니다.
  
  | 1열 (A) | 2열 (B) | 3열 (C) | 4열 (D) | 5열 (E) |
  | :--- | :--- | :--- | :--- | :--- |
  | **역명** | **위치 코드 (ID)** | **위치/시설명** | **연락처** | **구역코드 (도면파일명)** |
  | 가정역 | 211ES001 | 에스컬레이터 1호기 | 032-451-4311 | 211 |
  | 가정역 | 211EV005 | 엘리베이터 5호기 | 032-451-4311 | 211 |

---

## 2. 1단계: demo.html 변환기 접속

1. 웹 브라우저에서 `demo.html` 파일을 엽니다.
2. 화면 우측 상단의 **`data.js 변환기`** 버튼을 클릭합니다.
3. 화면이 **데이터 변환 및 위치·마커 설정 화면**으로 전환됩니다.

---

## 3. 2단계: 엑셀 데이터 입력 및 변환

### 💡 방법 A: 엑셀(Excel) 데이터 새로 붙여넣기
1. 준비한 엑셀 시트에서 입력할 데이터 영역(A열~E열)을 드래그하여 **복사(Ctrl+C / Cmd+C)** 합니다.
2. `demo.html` 화면의 **`엑셀 데이터 붙여넣기`** 텍스트 상자에 **붙여넣기(Ctrl+V / Cmd+V)** 합니다.
3. 아래의 **`변환하기`** 버튼을 클릭합니다.

> 팁: 데이터 형태를 미리 테스트해보고 싶다면 **`즉시 입력하기`** 버튼을 눌러 샘플 데이터를 로드할 수 있습니다.

### 💡 방법 B: 기존 data.js 파일 불러와서 수정하기
1. **`기존 data.js 불러오기`** 버튼을 클릭합니다.
2. 내 컴퓨터의 `data.js` 파일(또는 `.json` 파일)을 선택하여 로드합니다.

---

## 4. 3단계: 지도 좌표 및 마커 설정

엑셀 데이터를 변환하면 각 위치 항목에 대해 **도면상 위치(좌표)**와 **마커 스타일**을 지정할 수 있습니다.

1. **위치 코드 대상 선택**:
   - `위치 코드 대상` 드롭다운 목록에서 좌표를 설정할 장소(예: `211EV005 (가정역 엘리베이터 5호기)`)를 선택합니다.
   - 해당 구역의 도면 이미지(`images/211.jpg` 등)가 자동으로 지도 창에 로드됩니다.
   *(이미지가 뜨지 않는 경우 '수동 지도 등록'에서 컴퓨터 내 이미지 파일을 직접 선택할 수 있습니다.)*

2. **마커 종류 및 색상 지정**:
   - **마커 종류**: `위치 핀 (Pin)`, `레이더 비콘 (Beacon)`, `심플 도트 (Dot)`, `랜드마크 별 (Star)` 중 선택
   - **마커 색상**: `레드 (Rose)`, `블루 (Sky)`, `그린 (Emerald)`, `앰버 (Amber)`, `퍼플 (Purple)` 중 선택

3. **지도에서 위치 클릭**:
   - 표시된 도면 이미지에서 해당 시설/위치가 존재하는 곳을 **마우스로 직접 클릭**합니다.
   - 클릭한 위치에 설정한 마커가 짠! 나타나며, 좌표(`x_percent`, `y_percent`)가 자동으로 기록되고 변환 결과 코드에 즉시 반영됩니다.

---

## 5. 4단계: 결과 복사 및 data.js 파일 저장

1. 지도 설정이 끝나면 페이지 제일 하단의 **어두운 결과 상자(Code Output)**를 확인합니다.
2. `// [장소 데이터] 총 N건` 과 함께 완성된 `const placeData = { ... };` 코드가 생성되어 있습니다.
3. 상단의 초록색 **`결과 복사하기`** 버튼을 클릭합니다. ("복사 완료!" 문구가 뜸)
4. VS Code 또는 메모장 등의 편집기에서 프로젝트 폴더 내 **`data.js`** 파일을 엽니다.
5. 기존 내용을 **전체 선택(Ctrl+A / Cmd+A)** 후 **붙여넣기(Ctrl+V / Cmd+V)** 하여 덮어쓰고 **저장(Ctrl+S / Cmd+S)** 합니다.

---

## 6. 5단계: GitHub에 업데이트 반영하기

생성한 `data.js` 파일을 GitHub 저장소(Repository)에 업데이트하는 방법은 2가지가 있습니다. 편리한 방법을 선택하세요.

---

### 🌐 [방법 1] GitHub 웹사이트 이용하기 (초보자 추천)

1. 웹 브라우저에서 본인의 **GitHub 저장소 주소**로 이동합니다.
2. 저장소 파일 목록에서 **`data.js`** 파일을 클릭합니다.
3. 오른쪽 상단의 **연필 모양 아이콘 (Edit this file)**을 클릭합니다.
4. 기존 코드를 모두 지우고, 4단계에서 복사한 최신 코드를 **붙여넣기**합니다.
5. 우측 상단의 초록색 **`Commit changes...`** 버튼을 클릭합니다.
6. Commit message 란에 변경 내용(예: `data.js 위치 좌표 및 시설 데이터 업데이트`)을 작성한 뒤 **`Commit changes`**를 완료합니다.

---

### 💻 [방법 2] Git 명령어로 터미널에서 업데이트하기 (개발자/VS Code 사용자)

1. VS Code 터미널 또는 컴퓨터 터미널(Git Bash, Mac Terminal)을 엽니다.
2. 프로젝트 경로로 이동한 뒤 아래 명령어를 차례대로 입력합니다:

```bash
# 1. 변경된 data.js (및 신규 이미지) 스테이징
git add data.js

# 이미지 파일도 추가된 경우
git add images/

# 2. 커밋 메시지 작성
git commit -m "update: data.js 장소 데이터 및 좌표 갱신"

# 3. GitHub 원격 저장소로 업로드
git push origin main
```
*(기본 브랜치명이 `master`인 경우 `git push origin master` 입력)*

---

## 7. ❓ 자주 묻는 질문 (FAQ)

**Q. 변환하기 버튼을 눌렀는데 "올바른 탭 구분 데이터가 없습니다"라고 떠요.**  
👉 엑셀에서 데이터를 복사할 때 5개 열이 올바르게 선택되었는지 확인해 주세요. 엑셀의 한 행을 그대로 복사하면 각 항목 사이가 탭(`\t`)으로 구분됩니다.

**Q. 지도 이미지가 안 나타나요!**  
👉 5번째 열(구역코드)이 예를 들어 `211`이라면, 프로젝트의 `images/` 폴더 내에 `211.jpg` 파일이 존재하는지 확인해 주세요. 없는 경우 `수동 지도 등록` 버튼으로 임시 도면을 올려 위치를 찍을 수 있습니다.

**Q. GitHub에 올려도 웹사이트에 바로 반영이 안 되는 것 같아요.**  
👉 GitHub Pages를 사용하는 경우 배포까지 1~3분 정도 시간이 소요될 수 있습니다. 브라우저 창에서 **`Ctrl + F5` (강제 새로고침)**를 실행하여 캐시를 지우고 확인해 보세요.
